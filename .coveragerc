[run]
omit = 
       # ignore github actions install folders
       /home/runner/*
       # and /usr/local/<anything> as its outside the app
       /usr/local/*
       # anything inside .tox is outside scope too
       */.tox/*
